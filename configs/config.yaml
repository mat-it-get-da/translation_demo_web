# OpenAI API 기반 번역기 메인 설정
defaults:
  - model: gpt-4o-mini

# 기본 번역 설정
translation:
  source_language: "en"
  target_language: "ko"
  
# OpenAI API 설정
api:
  timeout: 60
  max_retries: 3

# 모델 공통 파라미터
model:
  temperature: 0.3  # 번역은 창의성보다 정확성이 중요
  max_tokens: 512   # 번역에 충분한 길이
  top_p: 0.9

